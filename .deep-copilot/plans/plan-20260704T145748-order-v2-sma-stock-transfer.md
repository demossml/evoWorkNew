# order-v2 (SMA-прогноз закупок) + Stock Transfer (переброска товара)

> Generated by Deep Copilot · Plan mode
> 2026-07-04T11:57:48.481Z

## 目标 / Goal
Реализовать две фичи из workApp, которых нет в нашем проекте: (1) полноценный /api/evotor/order-v2 с алгоритмом orderForecastV2 (ABC/XYZ, safety stock, seasonality, бюджет) вместо текущего stuba, (2) Stock Transfer — AI-эндпоинт переброски мёртвого товара между магазинами.

## 方案 / Approach
Перенести модульную архитектуру workApp в наш монолитный api.ts: создать src/services/ с orderForecastV2.ts, подключить в api.ts. Для Stock Transfer — создать новый эндпоинт в api.ts или выделить routes/ai.ts. Использовать Zod-валидацию (в workApp есть validation.ts), в нашем проекте пока inline-проверки.

## 步骤 / Steps
1. **Создать services/orderForecastV2.ts** — Скопировать orderForecastV2.ts из workApp, адаптировать импорты под наш проект (getDocumentsFromIndexFirst → наш метод получения документов, Evotor-клиент).
2. **Подключить order-v2 в api.ts** — Заменить заглушку items:[] на вызов runOrderForecastV2. Подключить валидацию OrderV2Schema.
3. **Создать /api/ai/director/stock-transfer** — Создать эндпоинт, который по deadstock-товарам с меткой move генерирует AI-рекомендации по переброске.
4. **curl-верификация обоих эндпоинтов** — Убедиться, что order-v2 возвращает полные данные (items, summary, assumptions). Проверить stock-transfer.

## 涉及文件 / Affected files
- `packages/backend/src/services/orderForecastV2.ts` — Создать — ядро алгоритма закупок (копия из workApp)
- `packages/backend/src/api.ts` — Заменить stub order-v2, добавить stock-transfer эндпоинт
- `packages/backend/src/evotor/index.ts` — Проверить наличие getProductStock (для orderForecastV2)

## 风险与缓解 / Risks
- orderForecastV2.ts зависит от getDocumentsFromIndexFirst (из workApp) — нужно адаптировать под нашу структуру index_documents
- orderForecastV2.ts использует Evotor API для получения stocks — нужно проверить, что наш Evotor-клиент поддерживает нужные методы
- Stock Transfer требует AI (Cloudflare Workers AI) — нужно убедиться, что AI-биндинг работает в нашем dev-окружении
- Оба эндпоинта требуют структуру env с c.var.evotor и c.var.db — в нашем проекте это c.get('evotor') и c.get('db')

## 下一步 / Next steps
- Переключиться в Agent mode
- Выполнить шаги 1-4
- Проверить через curl

