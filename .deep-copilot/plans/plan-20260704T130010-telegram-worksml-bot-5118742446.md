# Telegram-хранилище фото открытия (@worksml_bot → группа -5118742446)

> Generated by Deep Copilot · Plan mode
> 2026-07-04T10:00:10.903Z

## 目标 / Goal
Заменить R2: бэкенд отправляет фото через @worksml_bot в группу -5118742446 через sendPhoto, сохраняет только file_id в D1, при запросе дёргает getFile и отдаёт URL.

## 方案 / Approach
Добавить TELEGRAM_STORAGE_BOT_TOKEN + TELEGRAM_STORAGE_CHAT_ID в конфиг. Создать sendPhotoToTelegram (multipart → file_id). Создать D1 opening_photos. Заменить заглушку upload-photos. Добавить GET /api/openings/photos.

## 步骤 / Steps
1. **Добавить TELEGRAM_STORAGE_BOT_TOKEN + CHAT_ID в конфиг** — wrangler.toml [vars] + types.ts IEnv
2. **Создать sendPhotoToTelegram утилиту** — utils/sendPhotoToTelegram.ts: Buffer → fetch sendPhoto (multipart) → file_id
3. **Создать D1 таблицу opening_photos** — shop_uuid, user_id, date, category, slot_index, telegram_file_id, created_at
4. **Реализовать POST /api/uploads/upload-photos** — Заменить заглушку: принять файл → sendPhotoToTelegram → D1 → { success }
5. **Создать GET /api/openings/photos** — date+shopUuid → D1 → getFile → URL
6. curl-верификация обоих эндпоинтов

## 涉及文件 / Affected files
- `packages/backend/wrangler.toml` — Добавить TELEGRAM_STORAGE_BOT_TOKEN и TELEGRAM_STORAGE_CHAT_ID в [vars]
- `packages/backend/src/types.ts` — Добавить оба поля в IEnv
- `packages/backend/utils/sendPhotoToTelegram.ts` — Новая утилита: fetch sendPhoto (multipart) в группу -5118742446
- `packages/backend/src/api.ts` — Реализовать upload-photos + GET /api/openings/photos

## 风险与缓解 / Risks
- Фото видны участникам группы (-5118742446) — не приватный канал
- file_id вечен, но привязан к bot_token @worksml_bot — при смене токена всё сломается
- BOT_TOKEN в wrangler.toml сейчас от @TobaccoHouseNew_bot — добавляем отдельный TELEGRAM_STORAGE_BOT_TOKEN

## 下一步 / Next steps
- Переключиться в Agent mode
- Добавить токен и chat_id в wrangler.toml
- Создать sendPhotoToTelegram.ts
- Создать D1 таблицу
- Реализовать два эндпоинта
- curl-верификация

