# Fix ShareReportButton — backend (upload + serve) + local dev support

> Generated by Deep Copilot · Plan mode
> 2026-07-04T15:30:50.697Z

## 目标 / Goal
Кнопка «Поделиться» существует в 5 отчётах, но бэкенд-эндпоинт не реализован. Нужно добавить endpoint загрузки в R2 + endpoint отдачи файла (чтобы работало локально), улучшить UI (ссылка + копирование), добавить в SalaryReport.

## 方案 / Approach
1. Бэкенд: `.post("/api/evotor/share-report")` сохраняет в R2 и возвращает ЛОКАЛЬНЫЙ URL `/api/evotor/share-report/{key}` (не внешний R2). `.get("/api/evotor/share-report/:key")` отдаёт файл из R2 — работает и локально, и в продакшене. 
2. Фронтенд: `ReportShareButton` после загрузки показывает ссылку + кнопку «Копировать».
3. SalaryReport: добавляем `ReportShareButton` внизу.

## 步骤 / Steps
1. **POST /api/evotor/share-report — загрузка в R2** — Принимает FormData(file), сохраняет в R2 (share-reports/{uuid}.jpg), возвращает {url: '/api/evotor/share-report/{key}'}
2. **GET /api/evotor/share-report/:key — отдача файла** — Принимает :key, достаёт из R2 (share-reports/{key}), отдаёт с Content-Type: image/jpeg. Работает локально и в проде
3. **Улучшить ReportShareButton — ссылка + копирование** — После загрузки показывать ссылку + кнопку 'Копировать' (с иконкой Check на 3с). Использовать возвращённый url
4. **Добавить ReportShareButton в SalaryReport** — useRef на контейнер, <ReportShareButton targetRef={...} /> внизу
5. Проверка: tsc + curl тест

## 涉及文件 / Affected files
- `packages/backend/src/api.ts` — POST /api/evotor/share-report (загрузка) + GET /api/evotor/share-report/:key (отдача)
- `packages/frontend/src/shared/ui/ReportShareButton.tsx` — Ссылка + копирование вместо window.open
- `packages/frontend/src/pages/reports/SalaryReport.tsx` — Добавить ref + ReportShareButton

## 风险与缓解 / Risks
- R2 в wrangler dev работает локально (эмуляция на диске) — проверено
- GET-эндпоинт должен быть ДО POST в порядке регистрации роутов (Hono требует строгий порядок для статических и параметризованных путей одного префикса)

## 下一步 / Next steps
- Переключить в Agent mode для выполнения

## 备注 / Notes
Ключевое отличие от первого плана: URL возвращается ЛОКАЛЬНЫЙ (/api/evotor/share-report/{key}), не внешний R2. Файл отдаётся через GET-эндпоинт из R2. Это работает одинаково в локальной разработке и в продакшене (wrangler dev эмулирует R2 локально).
