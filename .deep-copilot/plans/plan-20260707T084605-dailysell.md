# Применить патч дизайн-системы + dailySell

> Generated by Deep Copilot · Plan mode
> 2026-07-07T05:46:05.878Z

## 目标 / Goal
Применить патч дизайн-системы + dailySell из отчёта агента Mac Mini. Патч консолидирует все цвета в семантические CSS-токены, удаляет дубликаты (glass theme, DynamicTableClassic), добавляет спарклайн выручки и правит баги в DailyBriefing.

## 方案 / Approach
Применить unified diff через git apply. Патч не конфликтует с нашими изменениями (server.ts, sellerEffectiveness.ts, другие строки api.ts). После применения: tsc --noEmit, vite build, перезапуск сервера, проверка health + страниц.

## 步骤 / Steps
1. **Сохранить патч в файл** — Записать полный diff в .deep-copilot/patches/design-system.patch
2. **Применить git apply** — cd ~/project && git apply .deep-copilot/patches/design-system.patch
3. **Проверить tsc --noEmit** — Убедиться, что все импорты разрешены, удалённые файлы не используются
4. **Пересобрать фронтенд** — npx vite build && cp -R dist ../backend/
5. **Перезапустить сервер и проверить health** — Убедиться что все страницы грузятся без ошибок
6. **Закоммитить и запушить** — git add -A && git commit && git push evoworknew ai:main

## 涉及文件 / Affected files
- `packages/backend/src/api.ts` — добавлен dailySell в ответ
- `packages/backend/src/evotor/index.ts` — dailySell в getSalesgardenReportData
- `packages/backend/src/evotor/utils.ts` — dailySell в D1-версии
- `packages/frontend/src/index.css` — новая дизайн-система, удалён glass theme
- `packages/frontend/tailwind.config.js` — добавлены success/warning токены
- `packages/frontend/src/config/theme.ts` — TELEGRAM_BG константы
- `packages/frontend/src/hooks/useTheme.ts` — централизованная тема Telegram
- `packages/frontend/src/shared/ui/tokens.css` — алиасы --ui-* на новые токены
- `packages/frontend/src/shared/ui/ReportKPIBar.tsx` — primary/default вместо 5 вариантов
- `packages/frontend/src/shared/ui/Sparkline.tsx` — переиспользуемый спарклайн
- `packages/frontend/src/hooks/dashboard/useRevenueTrend7d.ts` — 7-дневный тренд из dailySell
- `packages/frontend/src/widgets/home/TileWrapper.tsx` — удалён ringTone, единый accent
- `packages/frontend/src/pages/Home.tsx` — семантические токены, перестановка виджетов
- `packages/frontend/src/widgets/home/DailyBriefing.tsx` — PlanRing, правильные ключи API
- `packages/frontend/src/widgets/home/ui/PlanStatusWidget.tsx` — семантические токены
- `packages/frontend/src/widgets/navigation/ui/BottomNavigation.tsx` — группировка «Ещё», токены
- `packages/frontend/src/components/ui/button.tsx` — удалён (перенесён в @shared/ui)
- `packages/frontend/src/components/ui/card.tsx` — удалён (перенесён в @shared/ui)
- `packages/frontend/src/components/ui/input.tsx` — удалён (перенесён в @shared/ui)
- `packages/frontend/src/widgets/reports/ui/DynamicTableClassic.tsx` — удалён (дубликат V2)

## 风险与缓解 / Risks
- DashboardSummary.tsx использует другой TileWrapper (из shared/ui) — не затронут
- DynamicTableClassic удаляется вместе с импортом — чисто
- shared/ui уже экспортирует Button/Card/Input — импорты рабочие
- Новые CSS-переменные (--success, --warning) должны быть в tailwind.config.js — патч их добавляет

## 下一步 / Next steps
- Переключиться в Agent mode
- Применить патч через git apply
- Проверить tsc + vite build
- Перезапустить сервер, проверить страницы
- Закоммитить и запушить

