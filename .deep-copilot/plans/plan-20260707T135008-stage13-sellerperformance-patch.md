# Применение stage13-sellerperformance.patch

> Generated by Deep Copilot · Plan mode
> 2026-07-07T10:50:08.846Z

## 目标 / Goal
Применить патч stage13 на текущий код без конфликтов, проверить tsc + vite build, убедиться что sellerEffectiveness не сломан.

## 方案 / Approach
Патч большой (570KB), затрагивает ~30 файлов. Основное содержание: (1) рефакторинг SellerPerformance.tsx под новую систему метрик, (2) новый RevenueWidget, (3) dailySell в evotor, (4) замена хардкод-цветов на семантические Tailwind-токены, (5) удаление мёртвых файлов. Ключевой риск — конфликт в sellerEffectiveness.ts (файл, который мы активно правили).

## 步骤 / Steps
1. **Удалить мёртвые файлы (ai/prompts.ts, ai/schemas.ts, AnalyticsDashboards.tsx, getUniqueDocumentsByPeriodGrouped.ts)** — Пустые или неиспользуемые файлы — удаление безопасно.
2. **Применить бэкенд-изменения (evotor/index.ts, evotor/utils.ts, api.ts)** — dailySell — новое поле, обратная совместимость сохраняется.
3. **Применить изменения sellerEffectiveness.ts из патча** — КРИТИЧЕСКИЙ ШАГ: патч заменяет ВЕСЬ файл. Нужно убедиться, что наши правки (vapeShare fix, Rule 1 relative, buildCategoryMap guard) сохранены в версии патча. Если нет — смержить вручную.
4. **Применить фронтенд-изменения (SellerPerformance.tsx, RevenueWidget, Home.tsx, tailwind.config)** — Рефакторинг под новые типы, семантические цвета.
5. **Применить UI-изменения (App.tsx, SaveAsJpegButton, OrderForecastCards, остальные)** — Замена хардкод-цветов на tailwind-токены — безопасно.
6. **Проверка: tsc --noEmit + vite build** — Бэкенд и фронтенд должны компилироваться без ошибок.
7. **Дымовой тест API: seller-effectiveness?period=7** — Убедиться, что vapeShare, accShare, segmentation не сломаны.

## 涉及文件 / Affected files
- `packages/backend/src/ai/prompts.ts` — удалить
- `packages/backend/src/ai/schemas.ts` — удалить
- `packages/backend/src/db/repositories/getUniqueDocumentsByPeriodGrouped.ts` — удалить
- `packages/backend/src/api.ts` — dailySell
- `packages/backend/src/evotor/index.ts` — dailySell
- `packages/backend/src/evotor/utils.ts` — dailySell
- `packages/backend/src/services/sellerEffectiveness.ts` — КРИТИЧЕСКИЙ — полная замена
- `packages/frontend/src/App.tsx` — UI fix
- `packages/frontend/src/components/AnalyticsDashboards.tsx` — удалить
- `packages/frontend/src/pages/Home.tsx` — рефакторинг
- `packages/frontend/src/pages/SellerPerformance.tsx` — рефакторинг под новые типы
- `packages/frontend/src/widgets/reports/ui/RevenueWidget.tsx` — новый виджет
- `packages/frontend/tailwind.config.js` — success/warning токены

## 风险与缓解 / Risks
- sellerEffectiveness.ts: патч заменяет весь файл своей версией. Если в версии патча нет наших фиксов (vapeShare overwrite guard в buildCategoryMap, Rule 1 relative comparison с storeComboAvg), то эти правки будут потеряны. Нужно смержить вручную.
- Патч 570KB — apply_patch может не справиться целиком, лучше разбить на части.
- Удаление AnalyticsDashboards.tsx — проверить, что ни один другой файл его не импортирует.
- Пустые ai/prompts.ts и ai/schemas.ts — патч ожидает e69de29 (empty blob), но если файлы были изменены, удаление не сработает.

## 下一步 / Next steps
- Переключиться в Agent mode
- Применить патч по частям, начиная с безопасных (удаления, evotor, api)
- Смержить sellerEffectiveness.ts вручную
- tsc + vite build
- Дымовой тест API

