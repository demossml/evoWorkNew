# Полная миграция фронтенда из demossml/workApp — файл-в-файл

> Generated by Deep Copilot · Plan mode
> 2026-06-30T08:13:39.301Z

## 目标 / Goal
Скопировать каждый файл фронтенда из референсного репозитория demossml/workApp/packages/frontend в текущий проект, адаптировав API-клиент под наш бэкенд (@evo-app/backend).

## 方案 / Approach
Файл-в-файл копирование с референса. Сначала инфраструктура (конфиги, хелперы, API, хранилище), затем shadcn/ui компоненты, кастомные shared/ui компоненты, хуки, провайдеры, роутер, фичи, виджеты, страницы. На каждом этапе — проверка сборки.

## 步骤 / Steps
1. **Установка зависимостей** — tailwind-merge, radix-ui (collapsible, dialog, label, popover, select, slot, toast), class-variance-authority, framer-motion, react-day-picker v9, date-fns, chart.js, react-chartjs-2, html-to-image, zustand
2. **Конфиги: postcss, tailwind, index.html, vite.config.ts** — Скопировать буквально. vite.config.ts — с PWA, алиасами, ручным чанкированием
3. **index.css + shared/ui/tokens.css** — CSS-переменные, safe-area, dark mode, Telegram-отступы
4. **lib/utils.ts — функция cn** — clsx + tailwind-merge
5. **helpers/ (5 файлов)** — telegram.ts, analytics.ts, api.ts, backgroundUploader.ts, uploadQueue.ts — скопировать
6. **shared/model/dataSourceStore.ts** — Zustand store для источника данных (DB/ELVATOR)
7. **shared/api/ (6 файлов)** — client.ts (Hono RPC с трейсингом), endpoints.ts (адаптировать импорты Zod-схем), queryKeys.ts, queryClient.ts, invalidate.ts, index.ts
8. **components/ui/ — shadcn (13+ компонентов)** — Button, Card, Input, Badge, Select, Popover, Calendar, Dialog, Label, Toast, Collapsible, Sheet, Table
9. **shared/ui/ — кастомные компоненты** — Button, Badge, Card, Grid, StatCard, AlertCard, Input, Select, Textarea, ReportHeader, ReportKPIBar, ReportShareButton, states/ErrorBoundary, index.ts
10. **hooks/ (5 файлов)** — useApi.ts, useTheme.ts, userProvider.tsx, useTelegramFullscreenLayout.ts, useSimpleTelegramBackButton.ts
11. **app/ — провайдеры и роутер** — providers/AppProviders.tsx, router/AppRouter.tsx + index.ts файлы
12. **features/ — employees + dashboard** — employees/ui/RegisterUserCard.tsx + индексы, dashboard/model/homePageModel.ts
13. **widgets/ — navigation + home + reports** — navigation/ui/BottomNavigation.tsx, home/ (6 виджетов + barrel), reports/ (ShopSelector, GroupSelector, DynamicTable и др.)
14. **pages/ — все 17 страниц** — Home.tsx (основной дашборд с 14 виджетами), SellerPerformance.tsx, reports/ (13 файлов), opening/StoreOpeningPage.tsx, deadstock/DeadStock.tsx
15. **main.tsx + App.tsx + pwa.ts** — Скопировать с референса: PWAInstall, AppProviders, BottomNavigation
16. **Типы: vite-env.d.ts + global.d.ts** — Объявления для Vite PWA, Telegram WebApp
17. **Верификация: tsc + vite build** — 0 ошибок TypeScript, успешная сборка, проверка localhost:5174

## 涉及文件 / Affected files
- `packages/frontend/package.json` — добавить недостающие зависимости (редактировать)
- `packages/frontend/vite.config.ts` — заменить на референсный (редактировать)
- `packages/frontend/postcss.config.js` — создать заново (создать)
- `packages/frontend/tailwind.config.js` — заменить на референсный (редактировать)
- `packages/frontend/index.html` — заменить на референсный (редактировать)
- `packages/frontend/src/index.css` — заменить на референсный (редактировать)
- `packages/frontend/src/shared/ui/tokens.css` — создать (создать)
- `packages/frontend/src/lib/utils.ts` — создать cn (создать)
- `packages/frontend/src/helpers/*` — скопировать telegram, analytics, api, backgroundUploader, uploadQueue (создать/редактировать)
- `packages/frontend/src/shared/api/*` — адаптировать client, endpoints, queryKeys и др. (редактировать)
- `packages/frontend/src/components/ui/*` — создать все shadcn-компоненты (создать)
- `packages/frontend/src/shared/ui/*` — создать кастомные компоненты (создать/редактировать)
- `packages/frontend/src/hooks/*` — скопировать useApi, useTheme, userProvider, Telegram-хуки (редактировать)
- `packages/frontend/src/app/*` — заменить провайдеры и роутер (редактировать)
- `packages/frontend/src/features/*` — скопировать employees, dashboard/model (создать/редактировать)
- `packages/frontend/src/widgets/*` — скопировать navigation, home, reports (создать/редактировать)
- `packages/frontend/src/pages/*` — скопировать все 17 страниц (создать/редактировать)
- `packages/frontend/src/main.tsx` — заменить (редактировать)
- `packages/frontend/src/App.tsx` — заменить (редактировать)
- `packages/frontend/src/pwa.ts` — создать (создать)
- `packages/frontend/src/vite-env.d.ts` — создать (создать)

## 风险与缓解 / Risks
- Zod-схемы: референс импортирует из @work-appt/backend/src/contracts/. Нужно создать аналогичные в нашем бэкенде или убрать валидацию на первом этапе
- API-пути различаются: в референсе /api/employees/user, у нас /api/user. Нужно адаптировать endpoints.ts
- shadcn/ui требует много зависимостей — все есть в package.json референса
- Объём: ~60+ файлов. Потребуется несколько итераций

## 下一步 / Next steps
- Пользователь переключается в Agent mode
- Начинаем с Шага 1: установка зависимостей
- Далее Шаг 2: конфигурационные файлы
- Пошаговая верификация после каждого этапа

