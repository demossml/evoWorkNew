# Comprehensive fix plan for Evo App integration audit

> Generated by Deep Copilot · Plan mode
> 2026-06-30T13:54:43.649Z

## 目标 / Goal
Fix all issues found during a full audit of the frontend-backend integration, including: 4 missing API endpoints, 1 method mismatch, route path bugs, missing locale for calendars, broken HTML references, spelling/terminology errors, React anti-patterns, and missing error handling.

## 方案 / Approach
Fix issues in priority order: (1) backend API gaps — add 4 missing stubs + fix 1 method mismatch, (2) routing — fix dead-stock path, (3) HTML — fix favicon/apple-touch-icon references + lang attribute, (4) calendars — add ru locale to all 7+ Calendar instances, (5) spelling — fix ё consistency, "Твардоского", mixed A/B labels, (6) React — fix useEffect deps in useAccessoriesSales, fix refetch null flash in useSalesData, (7) error handling — add `!res.ok` checks to 5 endpoints, (8) vet run vitest to confirm all tests pass.

## 步骤 / Steps
1. **Backend: add 4 missing API stubs + 1 method fix** — Add stubs for POST /api/evotor/groups-by-shop, POST /api/evotor/order-v2, POST /api/evotor/stock-summary, POST /api/uploads/upload-photos. Fix GET/POST mismatch for /api/evotor/stock-report (frontend uses GET, add GET alias).
2. **Router: fix missing leading slash in dead-stock route** — Fix path from 'evotor/dead-stock' to '/evotor/dead-stock' so the dead stock page is reachable.
3. **index.html: fix lang, favicon, apple-touch-icon references** — lang='en' → lang='ru', favicon href /favicon.svg → /favicon.ico, apple-touch-icon href /apple-touch-icon.svg → /apple-touch-icon.png
4. **Calendar: add Russian locale to react-day-picker** — Import ru from date-fns/locale, pass locale={ru} to DayPicker in calendar.tsx. Affects 7+ pages.
5. **Spelling: fix ё, Твардоского, mixed caps** — Fix ё in 4 files (отчет→отчёт), fix 'Твардоского'→'Твардовского', fix 'Фин. отчет'→'Фин. отчёт', remove ALL-CAPS in AccessoriesWidget, replace mixed A/B labels.
6. **React fix: useAccessoriesSales missing dep** — Add params.enabled to useEffect dependency array in useAccessoriesSales.tsx.
7. **React fix: useSalesData null-on-refetch flash** — Remove `isRefetching ? null` branch; return stale data during background refetch.
8. **Error handling: add !res.ok checks to 5 endpoints** — Add `if (!res.ok) throw new Error(...)` to fetchMe, fetchEmployeeRole, fetchEmployeeNameAndUuid, fetchSchedules, fetchShops in endpoints.ts.
9. **Verify: run vitest integration tests** — Run `npx vitest run src/api.integration.test.ts` to confirm 21/21 pass.

## 涉及文件 / Affected files
- `packages/backend/src/api.ts` — Add 4 missing compat stubs + 1 GET alias for stock-report
- `packages/frontend/src/app/router/AppRouter.tsx` — Fix dead-stock route path
- `packages/frontend/index.html` — Fix lang, favicon, apple-touch-icon refs
- `packages/frontend/src/components/ui/calendar.tsx` — Add Russian locale prop
- `packages/frontend/src/pages/reports/SalesTodayReport.tsx` — Fix ё spelling
- `packages/frontend/src/pages/reports/StoreOpeningsAdminReport.tsx` — Fix ё spelling
- `packages/frontend/src/pages/reports/ProfitReportPage.tsx` — Fix ё spelling
- `packages/frontend/src/widgets/dashboard/cards/ExpensesCard.tsx` — Fix ё + ALL-CAPS labels
- `packages/frontend/src/widgets/home/AccessoriesWidget.tsx` — Fix ALL-CAPS + mixed A/B labels
- `packages/frontend/src/pages/SellerPerformance.tsx` — Fix 'Твардоского' → 'Твардовского'
- `packages/frontend/src/hooks/dashboard/useAccessoriesSales.tsx` — Add params.enabled to useEffect deps
- `packages/frontend/src/hooks/dashboard/useSalesData.tsx` — Remove null-on-refetch anti-pattern
- `packages/frontend/src/shared/api/endpoints.ts` — Add !res.ok error checks

## 风险与缓解 / Risks
- react-day-picker locale: v9 may require specific locale import syntax — verify at execution time
- useSalesData fix: consumers may depend on null-during-refetch behavior — test Home page after fix
- route path change: dead-stock link in BottomNavigation.tsx may also need updating
- spelling fix 'Твардоского': this may be an intentional proper noun — confirm with domain expert

## 下一步 / Next steps
- Hand this plan to Agent mode for execution
- Start dev servers and visually verify all pages render
- Check browser console for zero errors
- Navigate all routes and confirm no 404s

