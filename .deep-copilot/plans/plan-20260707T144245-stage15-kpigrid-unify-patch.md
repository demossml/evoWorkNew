# Применение stage15-kpigrid-unify.patch

> Generated by Deep Copilot · Plan mode
> 2026-07-07T11:42:45.784Z

## 目标 / Goal
Применить уникальную часть stage15-kpigrid-unify.patch: рефакторинг TileWrapper (убрать RingTone), CSS-миграция PlanStatusWidget, группировка бокового меню.

## 方案 / Approach
Stage15 = stage14 + 4 диффа. 95% патча уже в коде. Извлечь и применить только 4 уникальные секции через apply_patch.

## 步骤 / Steps
1. **Извлечь 4 уникальных диффа из stage15** — Нужно применить 4 уникальных диффа (остальное = stage13/stage14, уже в коде).
2. **Применить TileWrapper + TopProductWidget** — TileWrapper.tsx: RingTone удалён, ring-primary для expanded. TopProductWidget.tsx: ringTone проп удалён. Это конфликтов не даст.
3. **Применить PlanStatusWidget CSS-миграцию** — PlanStatusWidget.tsx: RGB хардкоды → success/warning/destructive токены. Проверить, нужны ли success/warning в tailwind.config.js (уже есть).
4. **Применить группировку бокового меню** — BottomNavigation.tsx: плоский список → группы с заголовками (Продажи, Финансы, Склад, Магазины). Новый импорт ClipboardCheck.
5. **tsc --noEmit + vite build** — Обе части должны компилироваться без ошибок.

## 涉及文件 / Affected files
- `packages/frontend/src/widgets/home/TileWrapper.tsx` — убрать RingTone
- `packages/frontend/src/widgets/home/TopProductWidget.tsx` — убрать ringTone проп
- `packages/frontend/src/widgets/home/ui/PlanStatusWidget.tsx` — CSS-миграция
- `packages/frontend/src/widgets/navigation/ui/BottomNavigation.tsx` — группировка меню

## 风险与缓解 / Risks
- PlanStatusWidget может иметь конфликты если файл локально изменён
- TileWrapper — если другие файлы используют ringTone проп, они сломаются (патч должен их все обновить)
- BottomNavigation.tsx — импорт ClipboardCheck может отсутствовать в lucide-react

## 下一步 / Next steps
- Переключиться в Agent mode
- Извлечь 4 диффа
- Применить
- Проверить

