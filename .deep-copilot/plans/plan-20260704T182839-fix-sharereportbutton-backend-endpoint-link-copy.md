# Fix ShareReportButton — backend endpoint + link copy UI

> Generated by Deep Copilot · Plan mode
> 2026-07-04T15:28:39.372Z

## 目标 / Goal
Кнопка «Поделиться» существует в 5 отчётах, но бэкенд-эндпоинт `/api/evotor/share-report` не реализован — кнопка падает с ошибкой. Нужно добавить endpoint загрузки в R2, улучшить UI компонента (ссылка + копирование), а в SalaryReport добавить кнопку «Поделиться» (отдельно от SaveAsJpegButton).

## 方案 / Approach
1. Бэкенд: добавить `.post("/api/evotor/share-report", ...)` — принимает FormData с полем `file`, сохраняет в R2 в папку `share-reports/`, возвращает `{ url }`. 
2. Фронтенд: улучшить `ReportShareButton` — после успешной загрузки показывать ссылку с кнопкой «Копировать» (сейчас просто `window.open`).
3. SalaryReport: добавить `useRef` на контейнер и `ReportShareButton` внизу, убрать обёртку `SaveAsJpegButton` (или оставить обе кнопки).

## 步骤 / Steps
1. **Add POST /api/evotor/share-report backend endpoint** — Принимает FormData(file), сохраняет в R2 (share-reports/{uuid}.jpg), возвращает {url}
2. **Improve ReportShareButton UI** — После загрузки показывать ссылку + кнопку копирования вместо window.open
3. **Add ReportShareButton to SalaryReport** — useRef на контейнер, кнопка внизу после всего контента
4. Verify: tsc backend + frontend, curl test endpoint

## 涉及文件 / Affected files
- `packages/backend/src/api.ts` — Добавить .post('/api/evotor/share-report') эндпоинт
- `packages/frontend/src/shared/ui/ReportShareButton.tsx` — Показывать share-ссылку + копирование после загрузки
- `packages/frontend/src/pages/reports/SalaryReport.tsx` — Добавить ReportShareButton с ref на контейнер

## 风险与缓解 / Risks
- R2_PUBLIC_URL должен быть настроен в .dev.vars (уже настроен)
- html2canvas может не захватить весь скроллируемый контент — проверить опцию height: element.scrollHeight

## 下一步 / Next steps
- Переключить в Agent mode для выполнения плана

## 备注 / Notes
ReportShareButton уже используется в 5 страницах: SaleReport, SalesTodayReport, SalestReportForThePeriod, ProfitReportPage, SellerPerformance. После добавления бэкенда все они начнут работать.
