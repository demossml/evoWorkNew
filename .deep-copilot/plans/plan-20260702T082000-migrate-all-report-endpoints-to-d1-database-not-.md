# Migrate all report endpoints to D1 database (not direct Evotor API)

> Generated by Deep Copilot · Plan mode
> 2026-07-02T05:20:00.903Z

## 目标 / Goal
Перевести все эндпоинты отчётов на работу через D1 (index_documents, shopProduct), а не напрямую через Evotor API. Основная проблема: sales-result сломан — getSalesSumQuantitySum ходит в Evotor API вместо D1, а shopProduct пустая.

## 方案 / Approach
1. Revert the sales-result endpoint to its original D1-based logic (undo my earlier direct-API fix). 2. Create a new D1-based function getSalesByProductFromD1 that reads from index_documents and aggregates REGISTER_POSITION transactions by commodityName (since commodityUuid is always undefined). 3. Modify the filtering to use commodityName matching instead of UUID matching. 4. Populate shopProduct table by running the updateProductsShope cron. 5. Migrate all other endpoints from direct Evotor API to D1 equivalents where D1 functions already exist.

## 步骤 / Steps
1. Revert sales-result to D1 logic + create D1 aggregation function
2. **Step 2** — Create getSalesByProductFromD1 in evotor/utils.ts: reads index_documents via getDocumentsBySalesPeriod, aggregates REGISTER_POSITION by commodityName. Update sales-result endpoint to use this + getProductsByGroup.
3. Populate D1 tables (run cron jobs)
4. **Step 4** — Execute updateProductsShope to fill shopProduct. Execute syncDocuments to fill index_documents if empty.
5. Migrate other endpoints to D1 where possible
6. **Step 6** — sales-garden-report: switch from evo.getSalesgardenReportData to D1 version in evotor/utils.ts. sales-today-graf: already uses getSalesDataG (D1-based).
7. Test all endpoints end-to-end

## 涉及文件 / Affected files
- `packages/backend/src/api.ts` — revert — restore D1-based sales-result logic with new D1 aggregation function
- `packages/backend/src/evotor/utils.ts` — add — new D1 function getSalesByProductFromD1 reading from index_documents
- `packages/backend/src/sync/cron.ts` — review — updateProductsShope populates shopProduct table
- `packages/backend/src/utils.ts` — review — parseTransactions, getDocumentsBySalesPeriod, getProductsByGroup

## 风险与缓解 / Risks
- commodityUuid всегда undefined в ответах Evotor — фильтрацию нужно делать по commodityName, а не по UUID
- index_documents может быть пустым — нужно сначала запустить syncDocuments cron
- shopProduct пустая — нужно запустить updateProductsShope cron
- Миграция всех эндпоинтов может затронуть работающие отчёты (sales-today, sales-garden-report)

## 下一步 / Next steps
- Switch to Agent mode
- Revert sales-result endpoint to D1-based logic
- Create D1 aggregation function that reads index_documents
- Run updateProductsShope cron to populate shopProduct
- Test full flow: группы + период + магазин = данные

