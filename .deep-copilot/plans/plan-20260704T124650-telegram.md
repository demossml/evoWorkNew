# Telegram-хранилище фото открытия (приватный канал)

> Generated by Deep Copilot · Plan mode
> 2026-07-04T09:46:50.683Z

## 目标 / Goal
Заменить R2-хранилище фото открытия: бэкенд отправляет фото в приватный Telegram-канал через sendPhoto (никто не видит), сохраняет только file_id в D1, при запросе дёргает getFile и отдаёт URL. Фото живут на серверах Telegram.

## 方案 / Approach
Два метода: (1) sendPhotoToTelegram — multipart в приватный канал → file_id. (2) getPhotoUrl(file_id) → URL через getFile. Слой хранения — D1 opening_photos. Фронтенд не меняется. Приватный канал как невидимое облачное хранилище.

## 步骤 / Steps
1. **Добавить TELEGRAM_STORAGE_CHAT_ID в конфиг** — wrangler.toml [vars] + types.ts IEnv
2. **Создать sendPhotoToTelegram утилиту** — utils/sendPhotoToTelegram.ts: Buffer → fetch sendPhoto (multipart) → file_id
3. **Создать D1 таблицу opening_photos** — shop_uuid, user_id, date, category, slot_index, telegram_file_id, created_at
4. **Реализовать POST /api/uploads/upload-photos** — Заменить заглушку: принять файл → sendPhotoToTelegram → сохранить file_id → { success: true }
5. **Создать GET /api/openings/photos** — date + shopUuid → D1 SELECT file_id → getFile → URL
6. curl-верификация

## 涉及文件 / Affected files
- `packages/backend/wrangler.toml` — Добавить TELEGRAM_STORAGE_CHAT_ID в vars
- `packages/backend/src/types.ts` — Добавить TELEGRAM_STORAGE_CHAT_ID в IEnv
- `packages/backend/utils/sendPhotoToTelegram.ts` — Новая утилита — sendPhoto multipart → file_id
- `packages/backend/src/api.ts` — Реализовать upload-photos + GET /api/openings/photos

## 风险与缓解 / Risks
- Фото до 20 MB через sendPhoto (наши сжатые JPEG 1200x800 — ~200 KB, укладываемся)
- file_id вечный, но привязан к bot_token — при смене токена старые file_id невалидны
- Приватный канал должен существовать и бот должен быть админом — иначе 403

## 下一步 / Next steps
- Создать приватный Telegram канал
- Добавить @evoHelperBot в админы канала
- Получить chat_id через @getidsbot
- Сообщить chat_id → я реализую

