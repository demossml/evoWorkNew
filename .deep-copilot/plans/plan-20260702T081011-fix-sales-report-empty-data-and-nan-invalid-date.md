# Fix Sales Report: empty data and NaN Invalid Date

> Generated by Deep Copilot · Plan mode
> 2026-07-02T05:10:11.293Z

## 目标 / Goal
The Sales Report page shows "NaN Invalid Date", all KPIs at 0, and "Нет данных по выбранным параметрам". The backend endpoint was already fixed to use Evotor API directly, but the fix may not have been hot-reloaded. Also need to investigate the date formatting issue.

## 方案 / Approach
1. Force-restart the wrangler dev server so the sales-result endpoint fix takes effect. 2. Test the endpoint directly with curl to verify it returns real data. 3. If the date issue persists, inspect the API response for missing startDate/endDate fields and fix the SaleReport.tsx formatPeriod to be defensive against invalid dates. 4. Verify the full flow in browser.

## 步骤 / Steps
1. Restart wrangler dev server to pick up backend changes
2. Test sales-result endpoint with curl for today's date
3. Check date formatting in SaleReport.tsx — fix if startDate/endDate missing
4. Verify report shows data in browser

## 涉及文件 / Affected files
- `packages/backend/src/api.ts` — already edited — sales-result endpoint fixed to use Evotor API directly
- `packages/frontend/src/pages/reports/SaleReport.tsx` — review — check formatPeriod for NaN date issue
- `packages/frontend/src/widgets/reports/ui/DynamicTableSalesReportV2.tsx` — reviewed — empty state message confirmed

## 风险与缓解 / Risks
- Wrangler may have silently failed to hot-reload the backend change — need restart
- The date NaN issue may be a separate frontend bug in formatPeriod/formatDate

## 下一步 / Next steps
- Switch to Agent mode
- Restart wrangler dev server
- curl-test the endpoint
- Fix any date formatting issues
- Verify in browser

