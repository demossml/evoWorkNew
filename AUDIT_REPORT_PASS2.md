# 🔍 AUDIT REPORT — Backend EvoWorkNew (Второй проход — ПЕРЕПРОВЕРКА)

> Дата: 2026-07-16 | Статус: ✅ Первый проход подтверждён
> Метод: перекрёстная проверка всех импортов и зависимостей

---

## 0. ПОДТВЕРЖДЕНИЕ НАХОДОК ПЕРВОГО ПРОХОДА

Все находки первого прохода **подтверждены**. Дополнительно выявлено:

### Новое: `d1/index.ts` — МЁРТВЫЙ КОД

Файл `d1/index.ts` (486 строк) **НИКЕМ НЕ ИМПОРТИРУЕТСЯ**. Проверено:
```bash
grep -rn "from.*d1/index\|from.*d1\"" packages/backend/src/ --include="*.ts"
# Результат: только drizzle-orm/d1 (библиотека), не наш файл
```

**Решение**: удалить `d1/index.ts` полностью. 486 строк мёртвого кода.

### Новое: 9 дубликатов в `utils.ts` — НИКТО НЕ ИМПОРТИРУЕТ

Проверено: `sync/cron.ts` импортирует эти функции из `./db.ts`, а не из `../utils.ts`. Значит версии в `utils.ts` — **мёртвый код**.

**Решение**: удалить ИЗ `utils.ts`, оставить в `sync/db.ts`. Обновлять импорты не нужно — никто не ссылается.

### Новое: `getDocumentsBySales` в `utils.ts` — чистая D1-функция

Она не использует Evotor API, только `db.prepare()`. Значит она должна быть в `evotor/utils.ts`. Сейчас `evotor/utils.ts` импортирует её из `../utils`.

**Решение**: перенести `getDocumentsBySales`, `getDocumentsBySalesPeriod`, `getDocumentsByCashOutcomeByPeriod` из `utils.ts` в `evotor/utils.ts`.

### Подтверждено: только 1 нарушение «D1-only»

Только `services/orderForecastV2.ts:164` вызывает `evotor.getProducts()`. Больше нигде в services/ нет нарушений.

### Подтверждено: 1 TODO во всей кодовой базе

Всего 1 `TODO`/`FIXME`/`HACK` — код чистый в этом смысле.

---

## 1. УТОЧНЁННАЯ КАРТИНА

### Полный список ДУБЛИКАТОВ (подтверждённый)

| # | Функция | utils.ts (строка) | sync/db.ts (строка) | Кто использует | Статус |
|---|---------|-------------------|---------------------|----------------|--------|
| 1 | `createProductsTableIfNotExists` | 1428 | 15 | Только sync/db.ts | Удалить из utils |
| 2 | `createSalaryTable` | 1083 | 218 | Только sync/db.ts | Удалить из utils |
| 3 | `getPlan` | (есть) | 137 | Только sync/db.ts | Удалить из utils |
| 4 | `getSalaryAndBonus` | (есть) | 196 | Только sync/db.ts | Удалить из utils |
| 5 | `getSalesDataByDate` | 1365 | (есть) | Только sync/db.ts | Удалить из utils |
| 6 | `saveSalaryData` | 1109 | 237 | Только sync/db.ts | Удалить из utils |
| 7 | `updateOrInsertData` | 1471 | (есть) | Только sync/db.ts | Удалить из utils |
| 8 | `updatePlan` | (есть) | (есть) | Только sync/db.ts | Удалить из utils |
| 9 | `delay` | 1182 | 7 | Только sync/db.ts | Удалить из utils |

### Нужно ПЕРЕНЕСТИ (не удалить!)

| # | Функция | Откуда | Куда | Причина |
|---|---------|--------|------|---------|
| 1 | `getDocumentsBySales` | utils.ts:2615 | evotor/utils.ts | Чистый D1, используется в evotor/utils.ts |
| 2 | `getDocumentsBySalesPeriod` | utils.ts:2577 | evotor/utils.ts | Чистый D1 |
| 3 | `getDocumentsByCashOutcomeByPeriod` | utils.ts | evotor/utils.ts | Чистый D1 |

### Мёртвый код (подтверждён)

| Файл | Строк | Статус |
|------|-------|--------|
| `d1/index.ts` | 486 | ❌ Никем не используется — УДАЛИТЬ |

---

## 2. ИТОГОВЫЙ ПЛАН (скорректированный)

### 🔴 Фаза 1: Удаление мёртвого кода (2-3 часа)
- [ ] Удалить `d1/index.ts` (486 строк) — мёртвый код
- [ ] Удалить 9 дубликатов из `utils.ts` — мёртвый код, никто не импортирует
- [ ] Обновить `CODEBUDDY.md` — отразить новую структуру

### 🟡 Фаза 2: Консолидация D1-методов (2-3 часа)
- [ ] Перенести `getDocumentsBySales`, `getDocumentsBySalesPeriod`, `getDocumentsByCashOutcomeByPeriod` в `evotor/utils.ts`
- [ ] Обновить импорты в `evotor/utils.ts` (убрать `from "../utils"`)
- [ ] Переименовать `evotor/utils.ts` → `d1/reports.ts`

### 🟢 Фаза 3: Типизация (3-4 часа)
- [ ] Создать `evotor/types.ts` → интерфейс `StockApiItem`
- [ ] Заменить 67 `as any` на правильные типы
- [ ] Обновить `DeadStockItem` в `types.ts`

### 🔵 Фаза 4: Рефакторинг (5-7 дней)
- [ ] Разбить `api.ts` на `api/*.ts`
- [ ] Разбить `utils.ts` на `utils/*.ts`
- [ ] Заменить `evo.getProducts` в `orderForecastV2.ts`

---

## 3. ФИНАЛЬНАЯ ОЦЕНКА

| Категория | Оценка | Δ | Комментарий |
|-----------|--------|---|-------------|
| Архитектура | ⭐⭐ | — | Подтверждено: монолит, свалка, мёртвый код |
| Чистота кода | ⭐⭐ | — | 486 строк мёртвого кода + 9 неиспользуемых дубликатов |
| Типобезопасность | ⭐⭐ | — | 67 `as any` подтверждено |
| Консистентность | ⭐⭐ | ▼ | Три разных utils-файла с пересекающейся ответственностью |

**Итог**: ⭐⭐ (2/5). Требуется системный рефакторинг.

---

## 4. ЧТО МЫ ВЫИГРЫВАЕМ ОТ РЕФАКТОРИНГА

| Действие | Выигрыш |
|----------|---------|
| Удаление `d1/index.ts` | −486 строк мёртвого кода |
| Удаление 9 дубликатов | −~500 строк дубликатов |
| Перенос 3 D1-функций | Чистая архитектура: D1 только в одном месте |
| Замена 67 `as any` | −67 потенциальных багов |
| Разбиение `api.ts` | Навигация, тестирование, merge-конфликты |
| Разбиение `utils.ts` | Понятная кодовая база для новых разработчиков |

---

*Аудит завершён. Оба прохода согласованы. Можно приступать к реализации.*
