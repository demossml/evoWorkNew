# Компактный отчёт по продажам: имя + подстрока с количеством и суммой

> Generated by Deep Copilot · Plan mode
> 2026-07-04T11:36:54.657Z

## 目标 / Goal
Увеличить плотность отображения товаров в отчёте по продажам. Сейчас каждая позиция занимает ~90-100px (заголовок + bordered box с двумя колонками). Нужно сократить до ~48px: имя товара + подстрока «X шт · Y ₽».

## 方案 / Approach
Только фронтенд. Меняется один файл DynamicTableSalesReportV2.tsx — рендеринг строки продукта. Бэкенд не трогаем — данные уже содержат productName, quantitySale, sum. Строка становится: имя (жирный, 14px), ниже — «X шт · Y ₽» (12px, серый). Разделитель — тонкая линия снизу.

## 步骤 / Steps
1. **Переработать DynamicTableSalesReportV2 — компактные строки** — Переработать рендеринг одной строки продукта: имя на первой строке, количество и сумма — на второй строке мелким шрифтом ниже. Убрать bordered box. Оставить сортировку, тёмную тему, анимацию.
2. **Визуальная проверка** — Проверить визуально на данных нескольких магазинов — убедиться, что на экране видно в 2-3 раза больше товаров

## 涉及文件 / Affected files
- `packages/frontend/src/widgets/reports/ui/DynamicTableSalesReportV2.tsx` — Переработать рендеринг строки — убрать bordered box, сделать компактно

## 风险与缓解 / Risks
- Пользователь, привыкший к текущему «V2» дизайну, может захотеть вернуться — тоггл «Новый/Старый» оставляем, старую V2 никуда не удаляем (она станет новой «Classic» логически, но это ок)

## 下一步 / Next steps
- Переключиться в Agent mode
- Применить изменения к DynamicTableSalesReportV2.tsx
- Открыть отчёт в Mini App и убедиться, что товаров на экране стало больше

