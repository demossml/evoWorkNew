# Применение stage14-stockhealth.patch

> Generated by Deep Copilot · Plan mode
> 2026-07-07T11:23:35.304Z

## 目标 / Goal
Применить stage14-stockhealth.patch — это stage13 (CSS-миграция) + новые фичи Stock Health (управление складскими остатками/здоровьем ассортимента).

## 方案 / Approach
Stage14 = Stage13 + Stock Health. Поскольку stage13 уже в коде, нужно: (1) извлечь только новые ~16KB диффов из stage14, (2) проверить их на конфликты с нашими изменениями, (3) применить через apply_patch.

## 步骤 / Steps
1. **Сравнить stage13 и stage14 — найти только новые изменения** — Stage14 = stage13 + ~16KB нового контента. Всё что уже применено из stage13 — пропустить.
2. **Извлечь только stock-health-специфичные диффы** — Новые файлы/компоненты связанные со Stock Health: виджеты остатков, мониторинг дед-стока, графики здоровья ассортимента.
3. **Применить новые диффы через apply_patch** — Только те файлы, которых ещё нет в коде. Уже изменённые (api.ts, sellerEffectiveness.ts) — проверить на конфликты.
4. **tsc --noEmit + vite build** — Обе части должны компилироваться без ошибок.
5. **Дымовой тест API** — Проверить seller-effectiveness и новый stock-health эндпоинт если есть.

## 涉及文件 / Affected files
- `packages/frontend/src/config/theme.ts` — новый файл (stage13)
- `packages/frontend/src/shared/ui/Sparkline.tsx` — новый файл (stage13)
- `packages/frontend/src/widgets/dashboard/DashboardSummary.tsx` — удалён и заменён
- `патч 586KB` — полный stage14 = stage13 + stock health

## 风险与缓解 / Risks
- Stage14 содержит ВСЕ диффы stage13 (570KB из 586KB). Большая часть уже применена — apply может конфликтовать на уже изменённых файлах (api.ts, index.ts, cron.ts).
- Нужно предварительно найти только NEW части (stock health diff).
- Может потребоваться ручной мёрж sellerEffectiveness.ts если патч его затрагивает.

## 下一步 / Next steps
- Переключиться в Agent mode
- Проанализировать stage14 на предмет stock-health-specific диффов
- Применить только новые части
- tsc + build + smoke test

