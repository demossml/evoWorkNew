# stage10-dead-code-removal — удаление мёртвого кода + темизация + dailySell

> Generated by Deep Copilot · Plan mode
> 2026-07-07T06:16:30.509Z

## 目标 / Goal
Применить патч stage10-dead-code-removal.patch (408 КБ), который включает:
1. **dailySell** — добавление выручки по дням в API финансового отчёта (3 файла бэкенда)
2. **Удаление dead-code** — ~10 компонентов-заглушек и неиспользуемых файлов
3. **Миграция на семантические Tailwind-токены** — замена хардкод-цветов (bg-blue-500, text-slate-900 и т.д.) на CSS-переменные (bg-primary, text-foreground, text-muted-foreground и т.д.)
4. **Добавление success/warning цветов** в tailwind.config.js

## 方案 / Approach
Патч чистый и непротиворечивый — удаляет только то, что уже не используется, заменяет импорты на актуальные (@shared/ui), и переводит все хардкод-цвета на семантические токены Tailwind. Применить через `git apply`, затем проверить `tsc --noEmit` и `vite build`. При ошибках — точечно поправить импорты.

## 步骤 / Steps
1. **Очистить .rej от предыдущего неудачного патча** — rm packages/frontend/src/index.css.rej
2. **Применить патч git apply** — git apply --whitespace=nowarn /Users/dmitrijsuvalov/Downloads/stage10-dead-code-removal.patch
3. **Проверить бэкенд: tsc --noEmit** — cd packages/backend && npx tsc --noEmit
4. **Проверить фронтенд: tsc --noEmit + vite build** — cd packages/frontend && npx tsc --noEmit && npx vite build
5. **Перезапустить сервер и проверить API** — Убить порт 3000, запустить сервер заново, curl /api/evotor/report/financial/today — должен содержать dailySell

## 涉及文件 / Affected files
- `packages/backend/src/api.ts` — добавлен dailySell в ответ
- `packages/backend/src/evotor/index.ts` — dailySell в типе и вычисление
- `packages/backend/src/evotor/utils.ts` — dailySell в типе и вычисление
- `packages/frontend/src/components/AnalyticsDashboards.tsx` — УДАЛЁН (360 строк dead-code)
- `packages/frontend/src/components/Topbar.tsx` — УДАЛЁН (463 строки dead-code)
- `packages/frontend/src/components/ReportUploader.tsx` — УДАЛЁН (125 строк dead-code)
- `packages/frontend/src/components/SchedulesView.tsx` — УДАЛЁН (dead-code)
- `packages/frontend/src/components/ui/button.tsx` — УДАЛЁН (импорт перенесён в @shared/ui)
- `packages/frontend/src/components/PlanStatusCards.tsx` — УДАЛЁН (re-export-заглушка)
- `packages/frontend/src/components/QuickActions.tsx` — УДАЛЁН (re-export-заглушка)
- `packages/frontend/src/components/RegisterUser.tsx` — УДАЛЁН (re-export-заглушка)
- `packages/frontend/src/components/TodayAlerts.tsx` — УДАЛЁН (re-export-заглушка)
- `packages/frontend/src/App.tsx` — хардкод-цвета → токены
- `packages/frontend/src/pages/Home.tsx` — цвета → токены + перестановка виджетов
- `packages/frontend/src/pages/reports/SaleReport.tsx` — цвета → токены
- `packages/frontend/src/pages/reports/SalaryReport.tsx` — импорт Button → @shared/ui
- `packages/frontend/src/pages/SellerPerformance.tsx` — variant → emphasis
- `packages/frontend/src/pages/opening/StoreOpeningPage.tsx` — цвета → токены, удалён setBackgroundColor
- `packages/frontend/src/pages/deadstock/DeadStock.tsx` — удалён theme/setBackgroundColor код
- `packages/frontend/src/widgets/reports/ui/SaveAsJpegButton.tsx` — импорт Button → @shared/ui, удалён theme код
- `packages/frontend/tailwind.config.js` — добавлены success и warning
- `packages/frontend/src/widgets/reports/ui/OrderForecastCards.tsx` — цвета → токены

## 风险与缓解 / Risks
- [object Object]
- [object Object]
- [object Object]
- [object Object]

## 下一步 / Next steps
- Переключиться в Agent mode
- Выполнить шаги плана: очистка .rej → git apply → tsc → vite build → перезапуск сервера

