# Apply frontend theme unification patch

> Generated by Deep Copilot · Plan mode
> 2026-07-04T19:19:27.567Z

## 目标 / Goal
Apply a large refactoring patch that consolidates design tokens, removes hardcoded colors, migrates UI component imports to @shared/ui, and simplifies widget ring/KPI color systems.

## 方案 / Approach
Apply the patch with apply_patch, then run TypeScript check to verify no broken imports or type errors.

## 步骤 / Steps
1. **Apply the full patch** — Use apply_patch with the entire diff. Deleted-file hunks are safe no-ops (files already deleted).
2. **Run TypeScript check** — pnpm tsc --noEmit or npx tsc --noEmit in frontend package to verify no broken imports.
3. **Fix any type errors** — If KPIVariant references remain, update them to KPIEmphasis.

## 涉及文件 / Affected files
- `packages/frontend/src/App.tsx` — edited
- `packages/frontend/src/index.css` — edited — major token rewrite
- `packages/frontend/src/shared/ui/ReportKPIBar.tsx` — edited
- `packages/frontend/src/shared/ui/index.ts` — edited
- `packages/frontend/src/shared/ui/tokens.css` — edited
- `packages/frontend/tailwind.config.js` — edited — adds success/warning
- `packages/frontend/src/config/theme.ts` — created
- `packages/frontend/src/components/ui/button.tsx` — deleted
- `packages/frontend/src/components/ui/card.tsx` — deleted
- `packages/frontend/src/components/ui/input.tsx` — deleted

## 风险与缓解 / Risks
- KPIVariant → KPIEmphasis: any remaining references to KPIVariant will cause TS errors
- calendar.tsx import path changed to ../../shared/ui/Button — verify path resolution
- config/theme.ts is a new file — verify directory exists

## 下一步 / Next steps
- Switch to Agent mode to apply the patch
- Run tsc --noEmit to verify
- Fix any remaining type errors if found

