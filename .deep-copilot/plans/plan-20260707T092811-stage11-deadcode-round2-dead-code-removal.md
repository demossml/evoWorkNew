# stage11-deadcode-round2 — расширенный dead-code removal + темизация

> Generated by Deep Copilot · Plan mode
> 2026-07-07T06:28:11.481Z

## 目标 / Goal
Применить stage11-deadcode-round2.patch (505 КБ) — расширенная версия stage10. Включает все изменения stage10 плюс дополнительные удаления dead-code и миграцию хардкод-цветов на семантические Tailwind-токены.

## 方案 / Approach
Stage10 уже применён через git apply --3way. Stage11 содержит надмножество тех же изменений — при переприменении --3way пропустит уже сделанные правки и применит только новые. После применения: tsc --noEmit на бэкенде и фронтенде, затем vite build.

## 步骤 / Steps
1. **Применить патч через git apply --3way** — git apply --3way stage11-deadcode-round2.patch
2. **Удалить dead-файлы, которые патч хочет удалить** — rm -f для файлов, которые не существуют или не удалились
3. Проверить бэкенд: tsc --noEmit
4. Проверить фронтенд: tsc --noEmit + vite build
5. Перезапустить сервер и проверить API

## 涉及文件 / Affected files
- `packages/frontend/src/components/ui/card.tsx` — УДАЛЁН (перенесён в @shared/ui)
- `packages/frontend/src/components/ui/chart.tsx` — УДАЛЁН (dead-code, 363 строки)

## 风险与缓解 / Risks
- [object Object]
- [object Object]

## 下一步 / Next steps
- Переключиться в Agent mode
- Выполнить git apply --3way → удалить dead-файлы → tsc → vite build → перезапуск сервера

